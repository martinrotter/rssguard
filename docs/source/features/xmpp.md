XMPP plugin
===========
RSS Guard offers basic `XMPP` support. It can fetch real-time `ATOM` entries via [XEP-0060](https://xmpp.org/extensions/xep-0060.html).

```{warning}
`XMPP` support was added recently and is considered to be highly experimental.

Expect bugs.
```